/* Estilos Gerais */
body {
  font-family: Arial, sans-serif;
  margin: 0;
  background: linear-gradient(to bottom, #0057b7, #06abff);
}

.header {
  text-align: center;
  padding: 40px 20px 20px;
  color: white;
}

/* ... (todos os estilos gerais, header, produtos, modal etc) */
